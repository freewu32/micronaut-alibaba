micronaut:
  application:
    name: micronaut-alibaba-nacos-test
  config-client:
    enabled: true
nacos:
  client:
    config:
      enabled: true
      server-addr: localhost
      data-id: database
      type: yaml